import React from 'react'
import { Text, View } from 'react-native'

const RenderSomeScreens = data => {
    return(
      <View>
        <Text>Hello {data.nama}</Text>
      </View>
    );
};

// cumanbisa sekali
export default RenderSomeScreens